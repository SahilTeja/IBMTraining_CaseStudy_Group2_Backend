

const Hello = (props) => {
    return (
        <span>
        <h3>**** it renders from Component->hello.js ****</h3>
        <h2>Hello {props.name}</h2>
        </span>
    );
}

export default Hello